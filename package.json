{
  "name": "shrineflow",
  "version": "0.6.86",
  "private": true,
  "description": "本機 JSON 版多平台社群發布工作台：建立內容、平台預覽與排程資料。",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test",
    "version:sync": "node scripts/update-version.js",
    "version:bump": "node scripts/bump-version.js",
    "css:split": "node scripts/split-css.js",
    "migrate:firestore": "node scripts/migrate-json-to-firestore.js",
    "migrate:firestore:plan": "node scripts/migrate-json-to-firestore.js --plan",
    "migrate:media:r2": "node scripts/migrate-uploads-to-r2.js",
    "migrate:media:plan": "node scripts/migrate-uploads-to-r2.js --plan"
  },
  "dependencies": {
    "@google/genai": "^2.16.0",
    "dotenv": "^16.4.7",
    "express": "^5.1.0",
    "firebase-admin": "^14.2.0",
    "multer": "^2.0.2"
  }
}
