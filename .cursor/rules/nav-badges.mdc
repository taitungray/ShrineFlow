---
description: 側欄數字只標待辦，庫存數不亮
alwaysApply: true
---

# 側欄數字：只標待辦

判準：**`.nav-badge` = 要處理的事。庫存／KPI／永遠有值的總數 → 不畫。0 就 `hidden`。**

實作：`refreshNavBadges()`（`public/modules/overview.js`）。

## 顯示（有值才亮）

| 分頁 | 數字 |
|---|---|
| 發布紀錄 | 失敗／重試／部分失敗／連線異常 |
| 審核佇列 | 待審 |
| 收件匣 | 未讀或待回 |

## 不顯示

- 內容：草稿篇數
- 日曆：已排程筆數
- 系統通知累積（沒已讀 UI，會一直亮）
- 素材庫、總覽、成效、活動、模板、設定等庫存

錯誤記錄可留 hidden slot；有未修才亮，勿用總筆數裝飾。

總覽卡片可寫庫存 KPI。那不是側欄。
